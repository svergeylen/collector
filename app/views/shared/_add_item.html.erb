
<p>
	<%= User.find(data[:user_id]).name %> vient d'ajouter :
</p>

<ul>
	<% Item.find(data[:item_ids]).each do |item| %>
		<li>
			<%= link_to series_path(item.series) do %>
				<%= item.series.name %>
				<%= "(n°"+friendly(item.number).to_s+")" if item.number %>
		 		: <%= item.name %>
		 	<% end %>
		</li>
	<% end %>
</ul>