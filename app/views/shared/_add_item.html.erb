
<p>
	<%= data[:user].name %> vient d'ajouter :
</p>

<ul>
	<% data[:items].each do |item| %>
		<li>
			<%= link_to item.series.name, series_path(item.series) %>
			<%= "(n°"+item.friendly_number+")" if item.number %> : 
		 	<%= link_to item.name, item %>
		</li>
	<% end %>
</ul>