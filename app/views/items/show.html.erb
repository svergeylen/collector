<div class="rounded-box">
	<div class="pull-right">
		<div class="btn-group">
			<%# get_link(@previous_id, "left", "Précédent") %>
			<%# get_link(@next_id, "right", "Suivant") %>
			<button type="button" class="btn btn-default dropdown-toggle btn-sm" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
				<span class="glyphicon glyphicon-cog" aria-hidden="true"></span>  <span class="caret"></span>
			</button>
			<ul class="dropdown-menu dropdown-menu-right">
				<li><%= link_to 'Modifier l\'élément', edit_item_path(@item) %></li>
				<li><%= link_to 'Supprimer l\'élément', @item, method: :delete, data: { confirm: 'Supprimer l\'élément ?' } %> </li>
			</ul>
		</div>
	</div>

	<h3><%= @item.name %></h3>

	<% if @attachments.present? %>
		
		<div class="row">
			<div class="col-md-8">
				<%= render partial: "shared/carousel", locals: {attachments: @attachments, active_slide_id: 0 } %>
			</div>
			<div class="col-md-4">
	<% end %>
			<p>Numéro : <%= @item.friendly_number if @item.number %></p>
			<% if @item.tags.present? %>
				<p>Tags : <%= @item.tags.collect {|t| link_to(t.name, t) }.join(", ").html_safe %></p>
			<% end %>
			<p>Ajouté par <%= link_to(@item.adder.name, @item.adder) if @item.adder %>, le <%= long_date(@item.created_at) %></p>
			
					
			<%# react_component "ItemQuantity", render(template: 'items/_quantity.json.jbuilder', locals: {item:  @item })  %>
			
			<% if @item.description.present? %>
				<h4>Description</h4>
				<%= sanitize @item.description %>
			<% end %>

			<h4>Ajouter une image</h4>
			<%= form_with(model: @item, local: true, :html => { :multipart => true }, class: "form-horizontal") do |form| %>
		      <%= form.file_field :attachments, multiple: true %>
		      <%= form.submit "Envoyer", class:"btn btn-default" %>
			<% end %>
		</div>

	<% if @attachments.present? %>
	</div>
	<% end %>
	
</div>
